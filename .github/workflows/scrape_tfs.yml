name: Scraper TFS

on:
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install -r requirements.txt
      - run: python Scraper_tfs_114.py
      - run: zip -r output_tfs.zip dorar_tafseer/
      - uses: actions/upload-artifact@v4
        with:
          name: output-tfs
          path: output_tfs.zip
          retention-days: 7
